# Check if the local environment contains an expected constant defining an API key

Check if the local environment contains an expected constant defining an
API key

## Usage

``` r
api_key_is_set()
```

## Value

TRUE if the key exists, FALSE otherwise

## Examples

``` r
api_key_is_set()
#> [1] FALSE
```
